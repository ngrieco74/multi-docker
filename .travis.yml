sudo: required
services: 
    - docker
    
before_install:
    - docker build -t ngrieco/react-test -f ./client/Dockerfile.dev ./client
    
script:
    - docker run -e CI=true ngrieco/react-test npm run test
      #docker run -e CI=true USERNAME/react-test npm test -- --coverage
    
after_success:
    - docker build -t multi-client -f ./client
    - docker build -t multi-nginx -f ./nginx
    - docker build -t multi-server -f ./server
    - docker build -t multi-worker -f ./worker
    # Log in in the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push ngrieco/udemy_examples:multi-client
    - docker push ngrieco/udemy_examples:multi-nginx
    - docker push ngrieco/udemy_examples:multi-server
    - docker push ngrieco/udemy_examples:multi-worker
    